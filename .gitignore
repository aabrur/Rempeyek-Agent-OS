# Secrets — NEVER commit
.env

# Runtime / local data
node_modules/
telemetry/*.jsonl
telemetry/logs/
apps/web/public/avatars/*
!apps/web/public/avatars/.gitkeep
runtime/

# Personal memory layer (lives on disk, not in this repo)
Obsidian Vault/
.obsidian/

# Generated artifacts
graphify-out/
apps/web/dist/
*.log

# Config backup written by the dashboard's Add Agent
agents.config.json.bak

# OS / editor
Thumbs.db
Desktop.ini
.DS_Store

# Secrets / dumps — NEVER commit (repomix embeds .env)
repomix-output.xml

# Personal config & tooling (copy agents.config.example.json instead)
agents.config.json
.claude/

# Runtime handshake files
telemetry/terms/

# Local scratch (graphify pipeline one-offs, personal canvas)
.worktrees/
do_semantic*.py
ast_and_cache.py
gen_subagents.py
prep_system.py
summarize*.py
subagents.json
system_prompt.txt
*.canvas
telemetry/memory-capture.json
