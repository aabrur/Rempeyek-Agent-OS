# apps/desktop

**Planned — not yet started.**

Native shell around `apps/web`: tray icon, native toasts (replacing the PowerShell
balloon hack), auto-start, frameless window. Candidate stack: Tauri (small footprint)
over Electron. The web app stays the single UI codebase; desktop only wraps it.

See [`docs/Roadmap.md`](../../docs/Roadmap.md).
