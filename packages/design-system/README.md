# @rempeyek/design-system

**Extraction target — code lives in `apps/web` today.**

Design tokens and rules: color token model, typography roles (Orbitron/Bahnschrift/
Cascadia Mono), spacing & radius scale, motion rules, reduced-motion policy.

Canonical spec: [`docs/Design-Bible.md`](../../docs/Design-Bible.md).
Current source: `:root` block in `apps/web/public/style.css`.
